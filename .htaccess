# Désactive MultiViews pour éviter les conflits de nom de fichier
Options -MultiViews

# Active la réécriture d'URL
RewriteEngine On

RewriteBase /

# Laisse passer les fichiers existants
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Redirige tout le reste vers index.html (pour React Router)
RewriteRule ^ index.html [L]
